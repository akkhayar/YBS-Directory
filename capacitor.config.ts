import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.akkhayar.ybsdirectory',
  appName: 'YBS Directory',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  },
};

export default config;
